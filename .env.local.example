# Copy to .env.local and fill in. .env.local is gitignored.

# Required — server-side only, never exposed to the client.
OPENAI_API_KEY=sk-...

# Optional — defaults to gpt-4-turbo. Set to gpt-5 / gpt-4o etc. if available.
OPENAI_MODEL=gpt-4-turbo
